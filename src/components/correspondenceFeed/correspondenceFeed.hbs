<div class="feed">
  <header class="feed__header">
    <div class="feed__contact">
      {{> Avatar avatar=contact.avatar width="34px" height="34px" alt=contact.name }}
      <span>{{contact.name}}</span>
    </div>
    <button type="button" class="menu-button" aria-label="Меню">
      <span class="point"></span>
      <span class="point"></span>
      <span class="point"></span>
    </button>
  </header>

  <div class="feed__main">
    {{#each messages}}
      <div class="message{{#if isOwn}} message--own{{/if}}{{#if image}} message--image-only{{/if}}">
        {{#if text}}<p>{{text}}</p>{{/if}}
        {{#if image}}<img src="{{image}}" alt="media" class="message__image" />{{/if}}
        <div class="message__time{{#if image}} message__time--darkened{{/if}}">{{time}}</div>
      </div>
    {{/each}}
  </div>

  <div class="feed__new-message">
    <form id="new-message">
      {{> Input id="message" formId="new-message" label="Сообщение" type="text" variant="outlined" customClass="feed__input" }}
      {{> Button icon=arrowIcon variant="icon" type="submit"}}
    </form>
  </div>
</div>
